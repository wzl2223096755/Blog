name = "academic-blog"
compatibility_date = "2024-01-01"

# KV 命名空间配置 - 用于费曼学习卡云端存储
[[kv_namespaces]]
binding = "FEYNMAN_DATA"
id = "YOUR_KV_NAMESPACE_ID"  # 需要在 Cloudflare 控制台创建后替换

# Pages 项目会自动使用这个配置
pages_build_output_dir = "dist"
